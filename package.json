{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^2.2.0",
    "turbo": "^1.6.3",
    "vite": "^3.2.4"
  }
}